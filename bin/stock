#!/bin/bash

readonly stock_codes="$@"
for code in $stock_codes
do
	echo -n "$code ";
	echo $code \
	| xargs -I{} curl -s 'https://kabutan.jp/stock/?code={}' \
	| grep -e kobetsu_data_table1_meigara -e kobetsu_data_table1_kabuka \
	| sed -E 's/<[^>]+>(.+)<[^>]+>/\1/g' \
	| xargs;
done
